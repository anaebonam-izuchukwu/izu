# TODO :check if the heights match
pips_height = int((input("Input the lisy of heights ").split(",")))
print(pips_height)
amount_height = len(pips_height)
height = int(pips_height) / amount_height
print(height)